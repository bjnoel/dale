{{ head|safe }}
{{ header|safe }}

<main class="{{ content_max_width }} mx-auto px-4 py-8">
  {{ breadcrumb|safe }}

  <h1 class="text-2xl font-bold text-green-900 mb-2">Fruit Tree Pollination Guide (Australia)</h1>
  <p class="text-gray-600 text-sm mb-6">Which fruit trees need a second tree to fruit, and which crop happily on their own. Written for Australian gardens, with evidence grades and a quick chart. Updated {{ today }}.</p>

  <p class="text-gray-700 mb-6">Pollination is the single most common reason a healthy fruit tree never fruits. Some trees set fruit with their own pollen; many need a different variety flowering at the same time; a few, like kiwifruit, need a separate male plant entirely. Sort this out before you buy, because you cannot fix it later without planting another tree and waiting years.</p>

  {{ how_to_read|safe }}

  {{ summary_table|safe }}

  {{ family_sections|safe }}

  {{ faq_section|safe }}

  {{ references_section|safe }}

  {{ related_guides|safe }}

  {{ treesmith_promo|safe }}
</main>

{{ footer|safe }}
</body>
</html>
