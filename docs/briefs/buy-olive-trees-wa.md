# Brief: rebuild the "Buy Olive Trees in Western Australia" page into an evidence-backed olive growing guide

## Status (verified 2026-06-01)

`treestock.com.au/buy-olive-trees-western-australia.html` is LIVE (HTTP 200, ~17 KB, rebuilt
daily) and is one of treestock's most visited entrance pages. Unlike the when-to-plant page,
it is NOT orphaned: it is generated by `tools/scrapers/build_species_state_pages.py`, which is
registered in `run-all-scrapers.sh` and rebuilt every day.

It is a TEMPLATE page. The same builder produces roughly 188 `buy-[species]-trees-[state].html`
combo pages (WA gets every species with 3+ in-stock items, QLD/NSW/VIC get the top 20 each).
The olive WA page is one instance of that template.

The page has three parts:
1. A live in-stock table (variety, nursery, price). This is the page's core job and it works.
   Keep it, and keep it high on the page.
2. A short amber "climate note" box near the top.
3. A long "Growing Olive in Western Australia" block at the bottom. This is the wall of text.

What is actually wrong (all verified by reading the builder and the live page):

- **The growing text is generic, not WA-specific, and uncited.** The "Growing Olive in Western
  Australia" block is just `species_info["description"]` (3275 chars) pulled from the olive entry
  in `fruit_species.json` (`build_species_state_pages.py` lines 242, 312-314, 358-359). The same
  text appears verbatim on the olive QLD, NSW and VIC pages and on `/species/olive.html`. Its
  heading promises Western Australia, but the body mentions South Australia and Victoria and says
  nothing specific to WA conditions, WA growing regions, WA pests, or WA harvest timing. It is one
  unbroken wall of text with no subheadings and no sources.
- **The climate note is miscategorised for olives.** `get_climate_note()` looks up
  `SPECIES_CLIMATE_CATEGORY["olive"]`, which is set to `"temperate"` (line 93), then renders
  `STATE_CLIMATE_NOTES["WA"]["temperate"]` (line 50): "South-west WA's mild winters suit temperate
  stone fruit and pome fruit ... Chilling hours may be lower, choose low-chill varieties." Olives
  are neither stone nor pome fruit and have effectively no winter-chill requirement, so the
  chill-hours advice is irrelevant and misleading on an olive page. (Grape is also tagged
  "temperate" and inherits the same wrong note, so the category model is too coarse.)
- **Copy-rule violations (no em or en dashes, per CLAUDE.md).** The price range is built with an
  EN dash: `f"${lo:.0f}–${hi:.0f}"` (line 257), so the live page shows "$25-$330" with a
  U+2013. The temperate climate note (line 50) contains an EM dash. Both are template-wide and
  affect every combo page, not just olive.
- **No citations, no FAQ, no structured data, no conversion path.** These high-traffic entrance
  pages have no sources, no FAQPage JSON-LD, and no alerts/Treesmith call to action.

So this is a "rebuild the editorial layer of a live template", NOT a re-home and NOT a 404 fix.
The plumbing is fine; the content is thin, generic, partly wrong, and breaks the copy rules.

## Goal

Replace the generic, uncited species blurb on this high-value template with evidence-backed,
WA-aware, scannable, cited olive growing guidance, with olive-in-WA as the flagship. Fix the
miscategorised climate note and the dash violations (which lifts every combo page). Keep the
live stock table as the primary content and keep it above the editorial section.

Architect it so the same content layer can later enrich the other top GSC entrance pages, not
just olive. Use `tools/scrapers/gsc_analysis.py` (it already tracks `-western-australia.html`
entrances) to confirm olive-WA's ranking and to pick the next species to enrich.

## Template and quality bar

Read these first and mirror them: `tools/scrapers/build_companion_guide.py` and the newer
`tools/scrapers/build_when_to_plant.py` (generated builders with per-item citations, a References
block, FAQPage JSON-LD, OG tags, internal `/species/` links validated against `fruit_species.json`,
no em or en dashes), plus `tools/scrapers/build_species_state_pages.py` itself and
`tools/scrapers/treestock_layout.py`.

- Accuracy here is about WA OLIVE GROWING. Wrong variety, climate, pest or harvest advice wastes
  years of a grower's time, so correctness is the top priority (the analogue of "no bad info").
- WA-specific, evidence-backed, and genuinely useful for a beginner getting into olives.
- Australia-wide site, so no "ships to WA" badges (show shipping restrictions instead). No em or
  en dashes anywhere (this is the bug that is already live, so the test must guard it).

## First step: capture the current page and template

Before changing anything, capture the live page (curl from the server to /tmp, the public URL
403s to bots: `ssh dale-server 'curl -s http://localhost/buy-olive-trees-western-australia.html'`)
and record its sections. Read `build_species_state_pages.py` end to end so the rebuild preserves
the working parts (stock table, nursery list, cross-state links, breadcrumb) and you can diff old
versus new. Note that the editorial body comes from `fruit_species.json`, so decide explicitly
whether the rich guide replaces that block on the combo pages only, or also upgrades
`/species/olive.html`.

## Substance to cover for olive in WA (verify all of it, do not assume)

- **Climate fit.** WA's south-west has a Mediterranean climate (hot dry summers, mild wet winters)
  that is close to ideal for olives. Olives need heat to ripen and do NOT need winter chill, so
  drop the chill-hours framing entirely. Note the warm dry inland and northern WA caveats
  (irrigation, very high heat).
- **WA growing regions.** Where olives are actually grown in WA (for example the Swan Valley,
  Perth Hills, Gingin/Moora, the Great Southern, Margaret River). Verify against WA sources.
- **Choosing varieties for WA.** Oil versus table versus dual-purpose, and which perform well in
  WA conditions. The page already lists Kalamata, Koroneiki, Arbequina, Manzanillo, Frantoio,
  Miniolea and others from live stock, so tie recommendations to varieties buyers can actually get.
  Cover self-fertility and the value of a pollinator (for example Pendolino).
- **Planting and soil.** Full sun, free-draining soil, sensitivity to waterlogging, and the WA
  reality of sandy soils (wetting agents, feeding, mulch). Container-grown olives can go in most of
  the year with summer water; cross-link to `/when-to-plant.html` for timing.
- **Water and feeding.** Drought tolerant once established, but young trees and fruiting trees need
  water through the dry WA summer. Realistic establishment watering.
- **Pests and diseases in WA.** Olive lace bug, anthracnose, peacock spot, black scale, and which
  pressures are higher or lower in WA. Mention WA quarantine context where relevant (it is why some
  eastern nurseries cannot ship here).
- **Harvest and curing.** WA harvest window (roughly autumn into early winter), green versus black,
  and that raw olives must be cured before eating.
- **Buying tips.** Grafted/known-variety stock over unnamed seedlings, typical pot sizes and prices,
  and a nudge to the live stock table above.

## Research (fan out agents, like the when-to-plant build)

Run a workflow fanning out per theme (climate fit and WA regions, variety selection, planting and
WA sandy soils, water and feeding, pests and diseases in WA, harvest and curing, buying and
pollination), and adversarially verify each claim against authoritative sources. Prefer .gov.au and
industry bodies: DPIRD WA (agric.wa.gov.au), the Australian Olive Association, Olives WA / the WA
olive industry, AgriFutures/RIRDC olive resources, and state ag departments; tag and down-weight
community/forum sources. Confirm every cited URL resolves. Synthesise into the guide, record what
you corrected from the old blurb, and verify the live stock varieties so recommendations match what
buyers can find.

## Engineering

- Introduce a per-species growing-guide content layer (for example
  `tools/scrapers/growing_guides/olive.json` or a `growing_guides.py` module) keyed by species slug,
  holding scannable, cited sections plus an optional per-state nuance block and a short FAQ. The
  combo-page template renders the rich guide when one exists and falls back to the current
  `fruit_species.json` description otherwise. This keeps the change additive and lets the next
  high-traffic species be enriched the same way.
- Render the guide as subheaded sections (Climate fit, Choosing varieties for WA, Planting, Water
  and feeding, Pests and diseases in WA, Harvest and curing, Buying tips), each with inline
  citations, plus a References block. Validate any internal `/species/` links against
  `fruit_species.json` so they cannot 404. Add FAQPage JSON-LD.
- Fix the climate-note miscategorisation: give olive a correct note (Mediterranean climate suits
  olives, no chill requirement, WA quarantine context) rather than the stone/pome-fruit text.
  Consider a "mediterranean" category so grape and olive stop inheriting the chill-hours line.
- Fix the dashes template-wide: the price range (line 257) and the climate notes. Replace en/em
  dashes with a hyphen, "to", commas or periods.
- Keep the in-stock table and nursery list as the primary content, above the editorial guide
  (do not push the table down). Optionally add an alerts call to action and the Treesmith promo
  (`render_treesmith_promo` from `treestock_layout`) BELOW the guide, never above the table.
- Add `tests/test_species_state_pages.py` (there is none today): the builder runs; the olive WA page
  contains WA-specific cited content; NO em or en dashes anywhere (this guards the live price-range
  bug); the olive climate note is not the stone-fruit/chill-hours text; FAQ JSON-LD parses; any
  references are https with rel=noopener; internal `/species/` links validate. Keep
  `python3 -m unittest discover tests/` green.

## Process

- Another agent may be working this repo concurrently: work in an isolated git worktree on its own
  branch (see memory feedback_parallel_agent_worktree), stage only your files, and finish by pushing
  and opening a PR rather than editing the shared working dir or merging to main unilaterally.
- Deploy like the other content builders: merge to main, then on the server (ssh dale-server) git
  pull, run tools/deploy.sh, run the scraper/build step into /opt/dale/dashboard, and rebuild the
  purged Tailwind CSS. NEVER scp.
- Verify the live URL returns 200 with the new WA-specific cited content and a fresh mtime, that the
  price range and notes no longer contain en/em dashes, and that the stock table still renders.

## Optional, related

- Once the layer exists, enrich the next two or three top GSC entrance pages the same way
  (use gsc_analysis.py to choose them).
- Consider rendering the rich guide on `/species/olive.html` too, so the species page benefits, not
  just the WA combo page.

Log a DEC entry and a public-ledger note when done.
